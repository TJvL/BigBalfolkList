name: Validate

# Nothing is generated any more, so nothing is committed by a bot. dances.json is the source
# and this only ever says yes or no, which is why it is safe to make it a required check.

on:
  push:
    branches: [main]
  pull_request:
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: validate-${{ github.ref }}
  cancel-in-progress: true

jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          # The whole history, so a pull request can be compared against the branch it
          # targets. That comparison is what catches a slug quietly disappearing.
          fetch-depth: 0

      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Tests
        run: python3 scripts/test_validate.py

      - name: Take the current list as a baseline
        if: github.event_name == 'pull_request'
        run: |
          git show "origin/${{ github.base_ref }}:dances.json" > /tmp/baseline.json

      - name: The site writes what the validator wants
        # The site regenerates the whole file when it opens a pull request. If its formatting
        # drifted from the validator's, every proposal it made would fail this very check.
        run: node scripts/check_canonical.mjs

      - name: The site compares names the way the validator does
        # The README tells consumers to copy one of the two implementations, so a name has to
        # fold and key the same in both or it resolves in one place and not the other.
        run: node scripts/check_fold.mjs

      - name: Check the list
        run: |
          if [ -f /tmp/baseline.json ]; then
            python3 scripts/validate.py --baseline /tmp/baseline.json
          else
            python3 scripts/validate.py
          fi
